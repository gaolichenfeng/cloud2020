server:
  port: 3366

spring:
  application:
    name: config-client
  cloud:
    config:
      label: dev
      name: config
      profile: dev
      uri: http://localhost:3344

  rabbitmq:
    host: 47.113.207.103
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
management:
  endpoints:
    web:
      exposure:
        include: "*"